nav.navbar.navbar-flat-tracker(ng-class="{'navbar-flat-tracker-inverse': user}")
    div.container
        div.navbar-header
            button.navbar-toggle.collapsed(type="button" data-toggle="collapse" data-target="#menu")
                span.sr-only Toggle navigation
                span.icon-bar
                span.icon-bar
                span.icon-bar
            a.navbar-brand(href="#") Flat Tracker

        div.collapse.navbar-collapse#menu
            ul.nav.navbar-nav.navbar-right
                li(ng-hide="!user")
                    a.dropdown-toggle(data-toggle="dropdown" role="button" aria-expanded="false" href="#") <i class="icon-male-sign-alt icon-2x"></i>
                        p Mon Compte
                    ul.dropdown-menu(role="menu")
                        li
                            a(ui-sref="user.profile") {{ user.username }}
                        li.divider
                        li
                            a(ui-sref="auth.logout") Déconnexion